import React from "react";
import Navbar from "../navbar/navbar";

const Header = () => {
  return (
    <header>
      {/* add search component, onclick goes to product details */}
      <Navbar />
    </header>
  );
};

export default Header;
